# 项目协作规则

1. 初始化项目时，阅读 `docs/init-project.md`。
2. 做任何任务之前，先阅读 `docs/indiehacking-project.md`。
3. 继续工作时，先阅读 `docs/计划文档.md` 和 `docs/架构文档.md`。
4. 做完以后，将当天完成的工作、验证结果和下一步写入计划文档。
5. 架构有变化时，同步更新架构文档；没有架构变化时也明确记录。
6. 不覆盖用户已有实现、配置或未提交改动。
7. 小程序所有页面都不要实现右上角的 `•••` / `◎` 胶囊按钮（menu-pill）。这是微信客户端自带的系统胶囊，由微信渲染在原生层。设计稿里画出它只是为了还原真机效果，不是需要实现的 UI；自己实现会和系统胶囊重叠。自定义导航栏（`navigationStyle: custom`）只做返回按钮和标题，右侧留空。
